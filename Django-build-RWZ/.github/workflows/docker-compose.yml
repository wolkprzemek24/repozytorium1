name: Docker Compose CI

on:
  push:
    branches:
      - RWZ
  pull_request:
    branches:
      - RWZ

jobs:
  docker-compose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build containers
        run: ls -la

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

     


      - name: Build containers
        run: docker compose -f ./mysite/docker-compose.yml build

      - name: Push image
        run: docker push szatan1v11/django-build:latest

      - name: Start containers
        run: docker compose -f ./mysite/docker-compose.yml up -d

      - name: Wait for services
        run: sleep 15

      - name: Show running containers
        run: docker ps

      - name: Django make migrations
        run: docker compose -f ./mysite/docker-compose.yml exec -T web python manage.py makemigrations

      - name: Django migrations
        run: docker compose -f ./mysite/docker-compose.yml exec -T web python manage.py migrate

      - name: Test app
        run: curl http://localhost:8000

      - name: Stop containers
        if: always()
        run: docker compose -f ./mysite/docker-compose.yml down